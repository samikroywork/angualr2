import { NgXUnlessDirective } from './ng-xunless.directive';

describe('NgXUnlessDirective', () => {
  it('should create an instance', () => {
    const directive = new NgXUnlessDirective();
    expect(directive).toBeTruthy();
  });
});
